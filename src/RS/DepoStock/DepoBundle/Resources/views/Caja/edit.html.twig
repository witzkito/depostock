{% extends "DepoBundle::layout.html.twig" %}

{% block body -%}
    <div class="container">
    <h1>Editar Caja</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
            <li>
                <span class="glyphicon glyphicon-chevron-left"></span>
                <a href="{{ path('caja', {'id' : entity.deposito.id}) }}">
                    Volver a la lista
                </a>
            </li>
        </ul>
    </div>
{% endblock %}
