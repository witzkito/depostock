{% extends "DepoBundle::layout.html.twig" %}

{% block body -%}
    <div class="container">
    <h1>Cuenta Corriente {{entity.cliente}}</h1>

    {{ form(form) }}

    <ul class="record_actions">
        <li>
            <span class="glyphicon glyphicon-chevron-left"></span>
            <a href="{{ path('cuentacorriente_show', {'id' : entity.cliente.id}) }}">
                Volver a la lista
            </a>
        </li>
    </ul>
     </div>
{% endblock %}
